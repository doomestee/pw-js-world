export enum LayerType {
    Background,
    Foreground,
    Overlay
}

export enum EffectId {
    JumpHeight,
    Fly,
    Speed,
    Invulnerability,
    Curse,
    Zombie,
    Poison,
    GravityForce,
    MultiJump,
    /**
     * Left = 1, Up = 2, Right = 3.
     */
    GravityDirection
}